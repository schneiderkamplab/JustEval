[project]
name = "JustEval"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "evaluate",
    "lm_eval[hf,vllm,api]",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "mypy"
]